


# assign function defaults to global env
# add function defaults to call
# convert backslashes (simple and escaped)
# make tribble from selected table
# ggExtra

iris

library(ggplot2)
plt <- ggplot(iris, aes(Sepal.Length, Sepal.Width)) + geom_point()

flow
